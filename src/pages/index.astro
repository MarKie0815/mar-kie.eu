---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Mar-Kie | Home" description="Welcome to the new Mar-Kie website">
	<section class="hero-section">
		<div class="container glass-panel">
			<div class="hero-content">
				<h1 class="hero-title">Welcome to Mar-Kie</h1>
				<p class="hero-subtitle">A modern, WordPress-free website with a distinctive section-based layout</p>
				<div class="hero-buttons">
					<a href="/blog" class="button primary">Read the Blog</a>
					<a href="/about" class="button secondary">Learn More</a>
				</div>
			</div>
		</div>
	</section>

	<section class="content-section">
		<div class="container glass-panel">
			<div class="section-header">
				<h2 class="section-title">About Us</h2>
				<p class="section-subtitle">Discover our story and mission</p>
			</div>
			<div class="section-content">
				<div class="text-image-grid">
					<div class="text-content">
						<p>Welcome to the new Mar-Kie website. We've redesigned our platform from the ground up, moving away from WordPress to a modern, high-performance solution that maintains all the functionality you love while adding new features and improvements.</p>
						<p>Our new website preserves the distinctive section-based layout where content areas appear to slide up as you scroll, creating an engaging and interactive experience.</p>
						<a href="/about" class="text-link">Learn more about us →</a>
					</div>
					<div class="image-content">
						<img src="https://via.placeholder.com/600x400" alt="About Mar-Kie" />
					</div>
				</div>
			</div>
		</div>
	</section>

	<section class="content-section">
		<div class="container glass-panel">
			<div class="section-header">
				<h2 class="section-title">Latest Blog Posts</h2>
				<p class="section-subtitle">Stay updated with our latest articles and insights</p>
			</div>
			<div class="blog-preview-grid">
				<div class="blog-preview-card">
					<div class="blog-preview-image" style="background-image: url('https://via.placeholder.com/400x300')"></div>
					<div class="blog-preview-content">
						<h3 class="blog-preview-title">Getting Started with Astro</h3>
						<p class="blog-preview-date">June 15, 2023</p>
						<p class="blog-preview-excerpt">Learn how we migrated from WordPress to Astro for better performance and developer experience.</p>
						<a href="/blog/getting-started-with-astro" class="text-link">Read more →</a>
					</div>
				</div>
				<div class="blog-preview-card">
					<div class="blog-preview-image" style="background-image: url('https://via.placeholder.com/400x300')"></div>
					<div class="blog-preview-content">
						<h3 class="blog-preview-title">Modern Web Development Techniques</h3>
						<p class="blog-preview-date">May 28, 2023</p>
						<p class="blog-preview-excerpt">Explore the latest techniques and best practices in modern web development.</p>
						<a href="/blog/modern-web-development" class="text-link">Read more →</a>
					</div>
				</div>
				<div class="blog-preview-card">
					<div class="blog-preview-image" style="background-image: url('https://via.placeholder.com/400x300')"></div>
					<div class="blog-preview-content">
						<h3 class="blog-preview-title">The Future of Content Management</h3>
						<p class="blog-preview-date">April 10, 2023</p>
						<p class="blog-preview-excerpt">How headless CMS and static site generators are changing the landscape of content management.</p>
						<a href="/blog/future-of-content-management" class="text-link">Read more →</a>
					</div>
				</div>
			</div>
			<div class="section-footer">
				<a href="/blog" class="button primary">View All Posts</a>
			</div>
		</div>
	</section>

	<section class="content-section">
		<div class="container glass-panel">
			<div class="section-header">
				<h2 class="section-title">Contact Us</h2>
				<p class="section-subtitle">Get in touch with our team</p>
			</div>
			<div class="contact-grid">
				<div class="contact-info">
					<p>Have questions or want to learn more about our services? Reach out to us using the contact form or through our email.</p>
					<div class="contact-method">
						<h3>Email</h3>
						<p>info@mar-kie.eu</p>
					</div>
				</div>
				<div class="contact-form">
					<form id="contact-form">
						<div class="form-group">
							<label for="name">Name</label>
							<input type="text" id="name" name="name" required />
						</div>
						<div class="form-group">
							<label for="email">Email</label>
							<input type="email" id="email" name="email" required />
						</div>
						<div class="form-group">
							<label for="message">Message</label>
							<textarea id="message" name="message" rows="5" required></textarea>
						</div>
						<button type="submit" class="button primary">Send Message</button>
					</form>
				</div>
			</div>
		</div>
	</section>
</Layout>

<style>
	/* Hero section styles */
	.hero-section {
		height: 80vh;
		min-height: 600px;
		display: flex;
		align-items: center;
		margin-bottom: var(--spacing-lg);
	}

	.hero-section .container {
		padding: var(--spacing-lg);
	}

	.hero-content {
		max-width: 800px;
		margin: 0 auto;
		text-align: center;
	}

	.hero-title {
		font-size: 3.5rem;
		font-weight: 700;
		margin-bottom: var(--spacing-sm);
		background: linear-gradient(90deg, var(--color-primary), var(--color-secondary));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		color: transparent;
	}

	.hero-subtitle {
		font-size: 1.5rem;
		color: rgba(255, 255, 255, 0.7);
		margin-bottom: var(--spacing-md);
	}

	.hero-buttons {
		display: flex;
		gap: var(--spacing-sm);
		justify-content: center;
	}

	/* Button styles */
	.button {
		display: inline-block;
		padding: 0.75rem 1.5rem;
		border-radius: 4px;
		font-weight: 500;
		text-align: center;
		transition: all 0.3s ease;
	}

	.button.primary {
		background-color: var(--color-primary);
		color: white;
	}

	.button.primary:hover {
		background-color: #2a75e8;
		transform: translateY(-2px);
	}

	.button.secondary {
		background-color: transparent;
		color: white;
		border: 1px solid rgba(255, 255, 255, 0.3);
	}

	.button.secondary:hover {
		background-color: rgba(255, 255, 255, 0.1);
		transform: translateY(-2px);
	}

	/* Section styles */
	.section-header {
		text-align: center;
		margin-bottom: var(--spacing-md);
	}

	.content-section {
		margin-bottom: var(--spacing-lg);
	}

	.content-section .container {
		padding: var(--spacing-md);
	}

	/* Text and image grid */
	.text-image-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: var(--spacing-md);
		align-items: center;
	}

	.text-content p {
		margin-bottom: var(--spacing-sm);
		color: rgba(255, 255, 255, 0.8);
	}

	.text-link {
		display: inline-block;
		margin-top: var(--spacing-xs);
		font-weight: 500;
		color: var(--color-primary);
	}

	.text-link:hover {
		color: var(--color-secondary);
	}

	.image-content img {
		border-radius: 8px;
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
	}

	/* Blog preview grid */
	.blog-preview-grid {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
		gap: var(--spacing-md);
		margin-bottom: var(--spacing-md);
	}

	.blog-preview-card {
		background: rgba(0, 0, 0, 0.2);
		border-radius: 8px;
		overflow: hidden;
		box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
		transition: transform 0.3s ease, box-shadow 0.3s ease;
		border: 1px solid var(--color-border);
	}

	.blog-preview-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
	}

	.blog-preview-image {
		height: 200px;
		background-size: cover;
		background-position: center;
		transition: transform 0.3s ease;
	}

	.blog-preview-card:hover .blog-preview-image {
		transform: scale(1.05);
	}

	.blog-preview-content {
		padding: var(--spacing-sm);
	}

	.blog-preview-title {
		font-size: 1.25rem;
		margin-bottom: var(--spacing-xs);
		color: white;
	}

	.blog-preview-date {
		color: rgba(255, 255, 255, 0.5);
		font-size: 0.875rem;
		margin-bottom: var(--spacing-xs);
	}

	.blog-preview-excerpt {
		color: rgba(255, 255, 255, 0.7);
		margin-bottom: var(--spacing-xs);
	}

	.section-footer {
		text-align: center;
		margin-top: var(--spacing-md);
	}

	/* Contact section */
	.contact-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: var(--spacing-md);
	}

	.contact-info {
		padding-right: var(--spacing-md);
	}

	.contact-info p {
		color: rgba(255, 255, 255, 0.8);
	}

	.contact-method {
		margin-top: var(--spacing-md);
	}

	.contact-method h3 {
		font-size: 1.25rem;
		margin-bottom: var(--spacing-xs);
		color: white;
	}

	.contact-method p {
		color: rgba(255, 255, 255, 0.7);
	}

	.form-group {
		margin-bottom: var(--spacing-sm);
	}

	.form-group label {
		display: block;
		margin-bottom: var(--spacing-xs);
		font-weight: 500;
		color: rgba(255, 255, 255, 0.9);
	}

	.form-group input,
	.form-group textarea {
		width: 100%;
		padding: 0.75rem;
		background: rgba(0, 0, 0, 0.2);
		border: 1px solid var(--color-border);
		border-radius: 4px;
		font-family: var(--font-family);
		font-size: 1rem;
		color: white;
	}

	.form-group input:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: var(--color-primary);
		box-shadow: 0 0 0 2px rgba(58, 134, 255, 0.2);
	}

	/* Responsive styles */
	@media (max-width: 768px) {
		.hero-title {
			font-size: 2.5rem;
		}

		.hero-subtitle {
			font-size: 1.25rem;
		}

		.text-image-grid,
		.contact-grid {
			grid-template-columns: 1fr;
		}

		.image-content {
			order: -1;
			margin-bottom: var(--spacing-sm);
		}

		.contact-info {
			padding-right: 0;
			margin-bottom: var(--spacing-md);
		}

		.hero-buttons {
			flex-direction: column;
			gap: var(--spacing-xs);
		}
	}
</style>

<script>
	// Handle contact form submission
	document.addEventListener('DOMContentLoaded', () => {
		const contactForm = document.getElementById('contact-form');
		
		if (contactForm) {
			contactForm.addEventListener('submit', (e) => {
				e.preventDefault();
				
				// In a real implementation, you would send the form data to a server
				// For now, we'll just show an alert
				alert('Thank you for your message! We will get back to you soon.');
				contactForm.reset();
			});
		}
	});
</script>
